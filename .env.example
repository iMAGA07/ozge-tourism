# Optional integrations for the inquiry form.
# The form works without these — submissions are logged on the server.

# Google Sheets — paste a deployed Apps Script Web App URL.
# Apps Script: doPost(e) → SpreadsheetApp.getActive().getSheetByName('Inquiries').appendRow([...])
GSHEETS_WEBAPP_URL=

# Telegram bot — create one with @BotFather, get the chat_id from /getUpdates
TG_BOT_TOKEN=
TG_CHAT_ID=
